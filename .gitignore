# deps
node_modules
.pnp
.pnp.js

# next
.next
out
build

# env
.env
.env.local
.env*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# editor
.vscode
.idea
.DS_Store

# prisma
prisma/migrations/dev/
prisma/*.db
prisma/*.db-journal

# docker volumes (only if mounted locally)
.docker-data
.vercel
.env.production.local

# The team workspace lives in the repo PARENT (Licenta/team), NOT in the code repo.
# Guard against agents that cd into the repo and write a relative team/ path.
/team/

# Keep prod builds out of the live dev .next cache (next build uses a separate distDir)
/.next-build/

# eval run output dumps (ad hoc result runs, not source)
lib/research/eval/out-*/
