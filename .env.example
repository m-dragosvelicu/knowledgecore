# --- Database ---
# Postgres connection string. Local docker-compose runs on host port 5440.
DATABASE_URL="postgresql://knowledgecore:knowledgecore@localhost:5440/knowledgecore?schema=public"

# --- Vector DB ---
# Qdrant local: see docker-compose.yml. Port 6433 is the HTTP API; 6434 is gRPC.
# API key is optional locally; required for any hosted Qdrant deployment.
QDRANT_URL="http://localhost:6433"
QDRANT_API_KEY=""

# --- Better Auth (email + password) ---
# Secret used to sign sessions. Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=""
# Base URL of the app. Local dev: http://localhost:3000 . On Vercel: the deployment origin.
BETTER_AUTH_URL="http://localhost:3000"

# --- LLM providers ---
# Anthropic — optional secondary provider.
ANTHROPIC_API_KEY=""
# Google GenAI — MANDATORY. KnowledgeCore runs LIVE-ONLY: every LLM-backed service
# requires this key and the app fails fast (throws) at startup without it. Set it
# here locally and in the Vercel project env for preview/production.
GOOGLE_GENAI_API_KEY=""
# Paralon — experimental third LLM provider (see lib/llm/paralon.ts). Status
# pending CEO confirmation; safe to leave blank.
PARALON_API_KEY=""

# --- Research APIs ---
# OpenAlex — MANDATORY for the academic source tier (lib/research/openalex.ts).
# The mailto polite-pool was deprecated on 2026-02-13; a real API key is required.
# The client appends api_key as a query parameter on every request.
# Credit-based limits apply: each search costs ~$0.001; monitor usage at
# https://openalex.org/accounts — the app throws a clear error if this key is absent.
OPENALEX_API_KEY=""

# Semantic Scholar (optional, increases rate limit).
SEMANTIC_SCHOLAR_API_KEY=""

# Tavily (https://tavily.com — generic web search fallback).
TAVILY_API_KEY=""

# Firecrawl (https://firecrawl.dev — web scraping/extraction fallback).
FIRECRAWL_API_KEY=""
